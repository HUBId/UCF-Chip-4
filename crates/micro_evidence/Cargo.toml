[package]
name = "micro_evidence"
version = "0.1.0"
edition.workspace = true

[dependencies]
blake3.workspace = true
serde = { workspace = true, optional = true }

[features]
default = []
serde = ["dep:serde"]
